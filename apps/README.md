# Apps

Reusable firmware applications and libraries live here after they are extracted from a project-specific Vitis workspace.

Keep firmware inside `projects/<date_topic>/vitis_repo/` while it is still tied to one XSA or class-day snapshot.
